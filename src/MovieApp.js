import './MovieApp.css';
import { AppRouting } from './routing/AppRouting';

const MovieApp = () => (
    <div className="MovieApp">
        <h1>Movie App</h1>
        <hr/>
        <AppRouting />
    </div>
);

export default MovieApp;
